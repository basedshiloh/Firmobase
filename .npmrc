# React 19 is newer than some libraries' declared peer ranges (e.g. recharts).
# legacy-peer-deps keeps npm install (local + Vercel) from failing on those
# soft peer mismatches without forcing incorrect resolutions.
legacy-peer-deps=true
